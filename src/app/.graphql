query Nodes($map: Float!) {
  nodes(map: $map) {
    id
    mapId
    system {
      id
      systemName
      class
      id
      trueSec
      statics {
        id
        name
        sourceClasses
        targetClass
        lifetime
        maxMass
        massRegen
        maxOnePass
      }
    }
  }
}
